{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python schedule_manage/manage.py migrate && gunicorn --chdir schedule_manage schedule_manage.wsgi",
    "restartPolicyType": "ON_FAILURE"
  }
}
